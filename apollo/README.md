# Backend API

start the database
```bash
$ docker-compose up
```

install the local dep and start the build
```bash
$ npm i
$ npm run build:dev
```

open another terminal and start the apollo server
```
$ npm start
```


